#include<stdio.h>
int main()
{
	char ch;
	printf("enter the alphabet:\n");
	scanf("%c",&ch);
	if(ch>='a' && ch <= 'z')
	{
		printf("given character is alphabet",ch);
	}
else
{
	printf("given character is not alphabet",ch);
}
}
